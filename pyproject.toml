[project]
name = "SentenceSplitter"
version = "0.1.0"
description = "Default template for PDM package"
authors = [
    {name = "Zunza5", email = "cazualdi@icloud.com"},
]
dependencies = ["torch>=2.10.0", "transformers>=5.3.0", "datasets>=4.7.0", "scikit-learn>=1.8.0", "numpy>=2.4.3", "jupyter>=1.1.0", "conllu>=6.0.0"]
requires-python = "==3.12.*"
readme = "README.md"
license = {text = "MIT"}


[tool.pdm]
distribution = false
